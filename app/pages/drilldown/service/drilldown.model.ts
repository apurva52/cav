export interface Drilldown {
  tierName: String;
  serverName: String;
  instanceName: String;
  startTime: number;
  endTime: number;
  btTransaction: String;
  isDir: boolean;   // need to change WIP
  isFile: boolean;  // need to change WIP
  isSelected: boolean; // need to change WIP
  mime: string;   // need to change WIP
  modified: string; // need to change WIP
  name: string;  // need to change WIP
  path: string;  // need to change WIP
  size: number;  // need to change WIP
  
}
// export interface DrilldownLoadPayload {
//   path: string;
// }


