import { async, ComponentFixture, TestBed } from '@angular/core/testing';

//import { PathAnalyticsFilterComponent } from './Path-analytics-filter.component';

describe('PathAnalyticsFilterComponent', () => {
  //let component: PathAnalyticsFilterComponent;
  //let fixture: ComponentFixture<PathAnalyticsFilterComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      //declarations: [ PathAnalyticsFilterComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    //fixture = TestBed.createComponent(PathAnalyticsFilterComponent);
    //component = fixture.componentInstance;
    //fixture.detectChanges();
  });

  it('should create', () => {
    //expect(component).toBeTruthy();
  });
});
